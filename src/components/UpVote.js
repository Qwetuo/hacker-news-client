// import React from 'react';

// const UpVote = (props) => {
//     return <p><button onClick={event => props.handleUpVoteBtn(event)}>up vote</button>{props.votes}</p>

// }

// export default UpVote;